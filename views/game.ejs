<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  <title>Vapour Library</title>
  <style>
    body {
      background: linear-gradient(to bottom, #1b2838, #0f1a24);
      color: #c6d4df;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    .navbar {
      background-color: rgba(15, 26, 36, 0.95) !important;
      backdrop-filter: blur(4px);
      border-bottom: 1px solid #2a475e;
    }

    .login-container {
      width: 90%;
      max-width: 500px;
      aspect-ratio: 1/1;
      margin: 50px auto;
      padding: 30px;
      background-color: #171a21;
      border-radius: 8px;
      box-shadow: 0 0 15px rgba(0, 0, 0, 0.8);
      box-sizing: border-box;
      display: flex;
      flex-direction: column;
      justify-content: center;
    }

    .login-container h2 {
      text-align: center;
      color: #66c0f4;
      margin-bottom: 30px;
    }

    .login-container input[type="text"],
    .login-container input[type="password"] {
      background-color: #2a475e;
      border: none;
      padding: 12px;
      margin-bottom: 20px;
      color: white;
      width: 100%;
    }

    .login-container button {
      background-color: #66c0f4;
      border: none;
      color: #0e1114;
      padding: 12px;
      width: 100%;
      font-weight: bold;
    }

    .login-container button:hover {
      background-color: #4582a7;
    }

    .comment-box,
    .comment-form {
      width: 60%;
      background-color: black;
      color: white;
      border: 2px solid crimson;
      padding: 15px;
      border-radius: 8px;
    }

    .comment-form input,
    .comment-form textarea {
      background-color: #1c1c1c;
      color: white;
      border: 1px solid crimson;
    }

    .comment-form input::placeholder,
    .comment-form textarea::placeholder {
      color: #ccc;
    }

    .comments-wrapper {
      width: 80%;
      background-color: #2e2e2e;
      border: 2px solid #b22222;
      border-radius: 10px;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    .game-details {
      background-color: #171a21;
      border: 1px solid #2a475e;
      border-radius: 10px;
      padding: 30px;
      margin-top: 30px;
      color: #c6d4df;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.6);
    }

    .game-details img {
      display: block;
      margin: 0 auto 20px;
      border-radius: 6px;
      box-shadow: 0 0 8px #000;
    }

    .game-details p {
      font-size: 16px;
      margin-bottom: 15px;
    }

    .game-image {
      width: 100%;
      max-width: 300px;
      height: auto;
      display: block;
      margin: 0 auto 20px;
      border-radius: 6px;
      box-shadow: 0 0 8px #000;
    }


    .btn-primary,
    .btn-warning {
      font-weight: bold;
      padding: 10px 20px;
      border-radius: 5px;
    }

    .btn-primary {
      background-color: #1a9fff;
      border: none;
    }

    .btn-primary:hover {
      background-color: #147eb7;
    }

    .btn-warning {
      background-color: #ffa500;
      border: none;
      color: #000;
    }

    .btn-warning:hover {
      background-color: #e59400;
    }

    .btn-success[disabled] {
      background-color: #2c3e50;
      color: #7f8c8d;
      cursor: not-allowed;
    }

    footer {
      background-color: #0f1a24;
      color: #7f8c8d;
      padding: 20px;
      text-align: center;
      margin-top: 40px;
    }
  </style>
</head>

<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Vapour Library</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse d-flex justify-content-between" id="collapsibleNavbar">
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/home">Home</a>
          </li> 
          <li class="nav-item">
            <a class="nav-link" href="/vapourstore">Store</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cart">Cart</a>
          </li>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
              More
            </a>
            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="/logout">Logout</a></li>
            </ul>
          </li>

          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
              Tag
            </a>
            <form id="tagSearchForm" action="/tagSearch" method="POST" style="display:none;">
              <input type="hidden" name="tag" id="tagInput">
            </form>

            <ul class="dropdown-menu">
              <li><a class="dropdown-item" href="#" onclick="submitTag('Role-playing')">Role-playing</a></li>
              <li><a class="dropdown-item" href="#" onclick="submitTag('Action')">Action</a></li>
              <li><a class="dropdown-item" href="#" onclick="submitTag('RPG')">RPG</a></li>
              <li><a class="dropdown-item" href="#" onclick="submitTag('Strategy')">Strategy</a></li>
              <li><a class="dropdown-item" href="#" onclick="submitTag('Adventure')">Adventure</a></li>
              <li><a class="dropdown-item" href="#" onclick="submitTag('Other/Misc')">Other/Misc</a></li>
            </ul>
          </li>
          
          <% if (user.role === "admin") { %>
            <li class="nav-item">
              <a class="nav-link" href="/addGame">Add Game</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/admin">Admin Panel</a>
            </li>
          <% } %>
        </ul>
        <form class="d-flex" role="search" action="/search" method="POST">
          <input class="form-control me-2" type="search" name="query" placeholder="Search" required />
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
      </div>
    </div>
  </nav>

  <div class="container game-details">
    <div class="text-center"><h2>Game Details</h2></div>
      <% if (game) { %>
        <p><b>Title:</b><br> <%= game.title %></p>
        <p><img src="<%= game.image %>" class="game-image"></p>
        <p><b>Description:</b><br> <%= game.desc %></p>
        <p><b>Price:</b><br> $<%= game.price %></p>
        <% if (OwnedGame.userId === user.id) { %>
          <button class="btn btn-success mt-3" disabled>Play</button>
        <% } else { %>
          <form action="/addToCart" method="POST" class="d-inline">
            <input type="hidden" name="gameId" value="<%= game.gameId %>">
            <button type="submit" class="btn btn-warning mt-3 me-2">Add to Cart</button>
          </form>

          <form action="/buy" method="POST" class="d-inline">
            <input type="hidden" name="gameId" value="<%= game.gameId %>">
            <button type="submit" class="btn btn-primary mt-3">Buy Now</button>
          </form>
        <% } %>


        <div class="container mb-4">
          <form action="/addComment" method="POST" class="comment-form mx-auto mb-4">
            <div class="mb-3">
              <textarea name="comment" class="form-control" rows="3" placeholder="Your comment" required></textarea>
            </div>
            <button type="submit" class="btn btn-danger">Post Comment</button>
          </form>

          <div class="comments-wrapper mx-auto p-3">
            <% for (let i = 0; i < userComments.length; i++) { %>
              <div class="comment-box mb-3">
                <h4><%= userComments[i].username %></h4>
                <p><%= userComments[i].comment %></p>
              </div>
            <% } %>
          </div>
        </div>

      <% } else { %>
        <p>No game found.</p>
        <a href ="/vapourstore">Back</a>
      <% } %>
  </div>

  <script>
    function submitTag(tag) {
      document.getElementById('tagInput').value = tag;
      document.getElementById('tagSearchForm').submit();
    }
  </script>
  
</body>
</html>
